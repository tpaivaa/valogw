{
  "name": "valogw",
  "description": "valogw serial and Mqtt gateway",
  "version": "0.0.1",
  "scripts": {
    "save": "tsc && git commit -am'save changes' && git push"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "mqtt": "^4.3.7",
    "serialport": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
