{
  "scripts": {
    "save": "tsc && git commit -am'save changes' && git push"
  },
  "dependencies": {
    "mqtt": "^4.3.7",
    "serialport": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  }
}
